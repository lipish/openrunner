# AI Rules

- UI: Do not use `window.alert()`, `window.confirm()`, or `window.prompt()` for user-facing messages.
- UI: Use the shadcn/ui-style `Dialog` component for success/error prompts (e.g., registration success).
